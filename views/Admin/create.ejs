<%- include('../partials/head.ejs') %>

    <body>
        <%- include('../partials/header.ejs') %>

            <main class="main-create container">
                <form action="/admin/crear" method="POST" enctype="multipart/form-data" class="form-create">
                    <label for="" class="form-label">Nombre :</label>
                    <input placeholder="Nombre de la planta" name="nombre" type="text" class="form-input">
                    <label for="" class="form-label">Clima :</label>
                    <select name="clima" id="category" class="select-create">
                        <% for (let i=0; i < climas.length; i++) { %>
                            <% if (climas[i].id===orquideas.clima_id) {%>

                                <option value="<%=climas[i].id%>" selected>
                                    <%=climas[i].tipoDeClima%>
                                </option>

                                <% } else { %>

                                    <option value="<%=climas[i].id%>">
                                        <%=climas[i].tipoDeClima%>
                                    </option>

                                    <% } %>

                                        <% } %>
                    </select>
                    <label for="" class="form-label">Precio</label>
                    <input placeholder="Clima de la planta" name="precio" type="text" class="form-input">
                    <label for="" class="form-label">Descripcion</label>
                    <input placeholder="Descripcion de la planta" name="descripcion" type="text" class="form-input">
                    <label for="" class="form-label">Categoria</label>
                    <select name="categoria" id="category" class="select-create">
                        <% for (let i=0; i < categorias.length; i++) { %>
                            <% if (categorias[i].id===orquideas.categoria_id) {%>

                                <option value="<%=categorias[i].id%>" selected>
                                    <%=categorias[i].categoria%>
                                </option>

                                <% } else { %>

                                    <option value="<%=categorias[i].id%>">
                                        <%=categorias[i].categoria%>
                                    </option>

                                    <% } %>

                                        <% } %>
                    </select>
                    <label for="" class="form-label">tamaño</label>
                    <select name="tamanio" id="category" class="select-create">
                        <% for (let i=0; i < tamanios.length; i++) { %>
                            <% if (tamanios[i].id===orquideas.tamanio_id) {%>

                                <option value="<%=tamanios[i].id%>" selected>
                                    <%=tamanios[i].tamanio%>
                                </option>

                                <% } else { %>

                                    <option value="<%=tamanios[i].id%>">
                                        <%=tamanios[i].tamanio%>
                                    </option>

                                    <% } %>

                                        <% } %>
                    </select>
                    <label for="" class="form-label">disponibilidad</label>
                    <input placeholder="disponibilidad de la planta" name="disponibilidad" type="text" class="form-input">
                    <label for="" class="form-label">flor</label>
                    <input placeholder="¿Esta con flor la planta?" name="flor" type="text" class="form-input">
                    <label for="" class="form-label">Seccion de home</label>
                    <select name="seccion" id="category" class="select-create">
                        <% for (let i=0; i < secciones.length; i++) { %>
                            <% if (secciones[i].id===orquideas.secciondehome_id) {%>

                                <option value="<%=secciones[i].id%>" selected>
                                    <%=secciones[i].seccion%>
                                </option>

                                <% } else { %>

                                    <option value="<%=secciones[i].id%>">
                                        <%=secciones[i].seccion%>
                                    </option>

                                    <% } %>

                                        <% } %>
                    </select>
                    <label for="" class="form-label">imagen 1</label>
                    <input name="imagen" type="file" class="">
                    <label for="" class="form-label">imagen 2</label>
                    <input name="imagen" type="file" class="">
                    <label for="" class="form-label">imagen 3</label>
                    <input name="imagen" type="file" class="">

                    <input type="submit" class="submit-create" value="Enviar">
                </form>
            </main>

            <%- include("../partials/footer.ejs") %>


                </html>